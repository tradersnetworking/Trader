# Copy values into deploy/.env on the VPS (do not commit secrets to git).
# Used by: npm run audit:workflow, npm run test:e2e:prod, npm run test:e2e:admin

E2E_INVESTOR_EMAIL=investor@akshayaexim.com
E2E_INVESTOR_PASSWORD=your-investor-password

E2E_ADMIN_EMAIL=your-superadmin@akshayaexim.com
E2E_ADMIN_PASSWORD=your-admin-password
